unit unit_ToolsFiles;

{ Stub. Registers list_measurements, get_measurement, save_project, load_project, list_projects (Tasks 9-11). }

interface

uses unit_MCPTools;

procedure RegisterFileTools(Registry: TToolRegistry);

implementation

procedure RegisterFileTools(Registry: TToolRegistry);
begin
  // Tools are added in a later task.
end;

end.
