unit unit_ToolsCalc;

{ Stub. Registers calc_reflectivity, evaluate_lines (Task 7 and 8). }

interface

uses unit_MCPTools;

procedure RegisterCalcTools(Registry: TToolRegistry);

implementation

procedure RegisterCalcTools(Registry: TToolRegistry);
begin
  // Tools are added in a later task.
end;

end.
