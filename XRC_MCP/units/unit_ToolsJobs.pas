unit unit_ToolsJobs;

{ Stub. Registers optimize_mirror, fit_xrr, job_status, job_result, cancel_job (Tasks 12-14). }

interface

uses unit_MCPTools;

procedure RegisterJobTools(Registry: TToolRegistry);

implementation

procedure RegisterJobTools(Registry: TToolRegistry);
begin
  // Tools are added in a later task.
end;

end.
